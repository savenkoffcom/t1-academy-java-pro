server:
  servlet:
    context-path: "/products-app"
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/task7
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

application:
  properties:
    http-debug: false